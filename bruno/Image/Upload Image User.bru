meta {
  name: Upload Image User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/image/uploadImageUser
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  file: @file()
}
