meta {
  name: Upload Image Network
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/image/uploadImageNetwork
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  networkId: 1
  file: @file()
}
