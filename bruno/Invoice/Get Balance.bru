meta {
  name: Get Balance
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/invoice/getBalance?networkId=1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  networkId: 1
}
