export default interface ProductInfo {
    profileId: number;
    networkId: number;
    slug: string;
    name: string;
    description: string;
    price: number;
    frequency: number;
    limit: number;
}