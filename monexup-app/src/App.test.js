// Placeholder Vitest sanity test.
// The original CRA boilerplate test ("renders learn react link") was removed
// during the Vite migration — it tested copy that hasn't existed since the
// initial scaffold. A real test suite is out of scope for this run.

import { describe, it, expect } from 'vitest';

describe('app sanity', () => {
  it('runs the test runner', () => {
    expect(true).toBe(true);
  });
});
