/**
 * @deprecated Removed in the dashboard redesign. The three KPI cards
 * (Vendas, Clientes, Faturas) are now rendered inline in
 * `DashboardPage/index.tsx` via the reusable `KpiCard` component.
 * Kept as an empty module stub so any stale import fails fast at the
 * type level rather than silently rendering legacy Bootstrap chrome.
 */
export {};
